images_dir: tests/data

model:
  conf_threshold: 0.2
  iou_threshold: 0.5
  task: slice
  vid_stride: 2
  weights: weights/last.pt

output_dir: tests/results/slice

sahi:
  overlap_ratio: 0.1
  slice_size_h: 640
  slice_size_w: 640
  
  track: true

change_analyze_conf: true
save_animations: true
