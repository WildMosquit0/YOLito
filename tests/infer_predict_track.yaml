images_dir: tests/data/slow.mp4

model:
  conf_threshold: 0.2
  iou_threshold: 0.5
  task: predict
  vid_stride: 2
  weights: weights/last.pt

output_dir: tests/results/predict_track

sahi:
  overlap_ratio: 0.1
  slice_size_h: 640
  slice_size_w: 640
  
  track: true

change_analyze_conf: true
save_animations: true
