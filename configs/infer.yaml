images_dir: demo/flying.MOV

model:
  conf_threshold: 0.2
  iou_threshold: 0.5
  task: predict
  vid_stride: 2
  weights: "/home/wildmosquit0/workspace/weights/mosquitoes/1000_each/weights/last.pt"

output_dir: demo/output

sahi:
  overlap_ratio: 0.1
  slice_size_h: 640
  slice_size_w: 640
  
  track: true

change_analyze_conf: true
save_animations: true